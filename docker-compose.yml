services:
  app:
    build: .
    image: vulnerable-spring-demo:latest
    container_name: vulnerable-spring-demo
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: default
    restart: unless-stopped
